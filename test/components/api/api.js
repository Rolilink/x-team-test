describe('Api', () => {
  it('should fetch data from server');
  it('should parse text data');
  it('should parse json data');
  it('should parse new-delimited json data');
  it('should reject with a 404 error when data is not fetched');
});
