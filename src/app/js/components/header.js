import React, { Component, PropTypes } from 'react';


export default class Header extends React.PureComponent {
  static propTypes = {}

  render() {
    return (
      <div>
        Header
      </div>
    );
  }
}
